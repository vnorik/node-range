/*var request = require('supertest');
var app = require(__dirname + '/../app');

describe('GET /', function() {
    it('Should contain test "Hello, Work!"', function (done) {
        request(app).get('/').expect(/Hello, Work!/, done);
    });
});*/